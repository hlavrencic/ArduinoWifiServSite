define(["wifiserv", "axios"], (wifiservDummy, axios) => {
  return {
    post(uri, data) {
      return new Promise((resolve, reject) => {
        setTimeout(() => {
          let random;
          switch (uri) {
         
            

            
          }
        }, 1000);
      });
    }
  };
});
